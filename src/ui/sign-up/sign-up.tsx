import { Box, Flex, Heading, VStack } from '@chakra-ui/react';

export default function SignUp() {
	return (
		<Flex minH={'100vh'} w={'100vw'} alignItems={'center'} justifyContent={'center'} maxW={'100vw'}>
			<Heading>Sign Up</Heading>
		</Flex>
	);
}
